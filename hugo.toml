languageCode = 'en-us'
title = 'title'
theme = ['hugo-cloak-email', 'mainroad']
[Author]
name = "blank"
bio = "This is a website for testing"

[Params]
	description = "Brandon's website"
	copyright = "Brandon G. Hacha"
	toc = false
	pager = false
	mainSections = ["about", "literature", "blog"]

	
[Params.sidebar]
	# home = "right"   # Configure layout for home page
  	# list = "right"   # Configure layout for list pages
 	# single = "right" # Configure layout for single pages
  	# Enable widgets in given order
  	widgets = ["affiliations"]
